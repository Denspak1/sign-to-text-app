# Sign-to-Text Banking Translator

This app uses MediaPipe Hands + Streamlit to translate simple hand gestures into banking transaction text.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py